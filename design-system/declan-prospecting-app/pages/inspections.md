# Inspections — page override (inherits MASTER)

- Filter row: single 38px control height for search/select/chips/date fields, centre-aligned
  (was mixed heights on flex-end — audit P5/P8 item); date eyebrows 12px.
- Attendee expand: chevron SVG that rotates (replaces ▾/▸ glyphs); attendee typeahead has the
  full keyboard path (MASTER).
- Report/Merge actions: copy + users SVG icons; report modal's Copy uses the copy icon.
- Status = colour + text: Active/Archived chips stay the shared segmented control; archived
  pill neutral grey (entity, not state); overdue/due wording never colour-only.
