node_modules/
dist/
*.log
.DS_Store

# Code-signing material — never commit private keys / certs
signing/
*.pfx
*.p12

# Local editor / tooling state
.claude/

# NEVER publish contacts data or exports, even if they end up in this folder
prospecting-data.json
app-config.json
Declan_Contacts_*.xlsx
Declan_Contacts_*.csv
*.xlsx
*.csv
